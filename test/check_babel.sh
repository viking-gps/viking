#!/bin/sh
# Run the test_babel program
#  Don't actually care what the output is (i.e. don't care if gpsbabel is available or not)
#  Just confirm that the program runs at all
./test_babel 1 0 1 0 1 0
